# Task 1 - GitHub, Virtual Environment, and Linux Basics

## Description
This folder contains the work for Task 1 of the Synergy Task Phase. It includes a Python virtual environment setup, dependency management using `requirements.txt`, Linux command documentation, setup logs, and a basic Python script.

## Folder Contents
- `src/hello.py` - Basic Python script for Task 1
- `requirements.txt` - Python dependencies used in the virtual environment
- `setup_log.md` - Record of commands used during setup
- `linux_commands.md` - Documentation of required Linux commands
- `data/sample.txt` - Sample data file

## Setup Instructions

### 1. Create the virtual environment
From the repository root:

```bash
python3 -m venv task_1/venv